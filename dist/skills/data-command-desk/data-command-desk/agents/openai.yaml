interface:
  display_name: Data Command Desk
  short_description: Data workflow orchestrator for modeling, ingestion, streaming,
    transformation, orchestration, quality, lineage, metrics, governance, retention,
    migration, cost, and data incidents.
  default_prompt: Ask this suite for a data outcome on a named dataset, pipeline,
    or metric, such as modeling a mart with a declared grain, making a failing load
    idempotent and reconciled, reconciling two dashboards that disagree on the same
    KPI, or scoping the blast radius of bad data already in production.
  icon: null
  primary_color: null
  icon_small: null
  icon_large: null
policy:
  allow_implicit_invocation: true
  products:
  - generic
_generated:
  profile: frontier-2026-07
  vendor: generic
_deprecated: Legacy path. Use agents/generic.yaml. Removed after 2026-11-01.
