# Connector Preflight

Use GitHub for repo state, source files, branches, issues, pull requests, workflows, deployment manifests, dependencies, and tests. Use docs/uploads for product briefs, design decisions, content inventories, brand rules, analytics notes, policy requirements, and stakeholder decisions. Use communication sources only as decision context.

If a required connector is unavailable, produce a diagnostic or `Workflow Halt` instead of inventing facts.
