interface:
  display_name: Red Team Eval Desk
  short_description: Plans and analyzes defensive adversarial AI tests and regression
    candidates.
  default_prompt: Use invoke red-team-eval-desk to plan or analyze adversarial AI
    testing for jailbreaks, prompt injection, data exfiltration, unsafe autonomy,
    over-permissioned tools, and policy evasion with explicit scope boundaries, severity
    rubrics, mitigations, and downstream handoffs.
  icon: 🛡️
  primary_color: '#dc2626'
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
  products:
  - generic
_generated:
  profile: frontier-2026-07
  vendor: generic
_deprecated: Legacy path. Use agents/generic.yaml. Removed in v1.1.0.
