interface:
  display_name: Agent Architecture Desk
  short_description: Designs bounded AI agent architecture, autonomy limits, state,
    memory, tools, approvals, observability, and handoffs.
  default_prompt: Use invoke agent-architecture-desk to design or harden AI agent
    architecture with explicit agency ceiling, loop design, state and memory strategy,
    tool routes, approval gates, retry and halt policy, observability hooks, eval
    scenarios, and low-token implementation handoffs.
  icon: 🕸️
  primary_color: '#2563eb'
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
  products:
  - generic
_generated:
  profile: frontier-2026-07
  vendor: generic
_deprecated: Legacy path. Use agents/generic.yaml. Removed after 2026-11-01.
